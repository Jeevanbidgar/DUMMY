name: Test Action

on:
  push:
    branches:
      - main  # or any branch you want to trigger the action

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Display Email (using echo)
        run: echo "Your email is:"22f2000831@ds.study.iitm.ac.in"

      - name: Display Email (using cat << EOF)  # Alternative
        run: |
          cat << EOF
          Your email is: xxfxxxxxx@ds.study.iitm.ac.in
          EOF

      - name: Display Email (using environment variable) # Best practice
        env:
          EMAIL: "xxfxxxxxx@ds.study.iitm.ac.in"
        run: echo "Your email is: $EMAIL"
